# langchain-practice
langchain-practice

创建虚拟环境, 并且安装指定langchain版本
```shell
pip install langchain==0.3.25
pip install langchain_openai==0.3.25
pip install langchain_community==0.3.25
pip install bs4
pip install faiss-cpu
pip install openai==1.81.0
```

密钥来自于：https://www.closeai-asia.com

